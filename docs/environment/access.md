# Access

How I access these machines and where credentials live — not the credentials themselves.

## SSH / remote access
_(to fill in)_

## Secrets
Where are secrets actually stored (e.g. a password manager, Ansible Vault, SOPS)? Never commit real secrets to this repo — see `.gitignore` for patterns that are already excluded.

_(to fill in)_
